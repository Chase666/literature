<h1>Все рецензии</h1>
<% @reviews.each do |review| %>
  <div>
    <div>
      <p><%= link_to review.title,review_path(review)%></p>
    </div>
  </div>
<% end %>
<p> <%= link_to "Новая рецензия", new_review_path %> </p>
